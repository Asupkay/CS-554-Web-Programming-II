<article class="container">
    <div class="page-header">
        <h1>Alex's Portfolio of Ball Wonders</h1>
    </div>
    <div class="row">
        <div class="col-sm-3 sidenav">
            {{#each products}}
            <a href="#{{this.id}}">{{this.name}}</a><br> 
            {{/each}}
        </div>
        <div class="col-sm-9">
            <div class="row">
                {{#each products}}
                <div class="col-12 col-md-6 col-lg-4" id="{{this.id}}">
                    <div class="card">
                        <img class="card-img-top" src="{{this.img}}" alt="Card image cap">
                        <div class="card-body">
                            <h2 class="card-title">{{this.name}}</h2>
                            <p class="card-text">{{this.description}}</p>
                            <button type="button" class="btn btn-dark" data-toggle="modal" data-target="#modal-{{this.id}}">More Detail</button>

                            <div class="modal" id="modal-{{this.id}}" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel-{{this.id}}" aria-hidden="true">
                              <div class="modal-dialog" role="document">
                                <div class="modal-content">
                                  <div class="modal-header">
                                    <h2 class="modal-title" id="exampleModalLabel-{{this.id}}">{{this.name}}</h2>
                                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                                      <span aria-hidden="true">&times;</span>
                                    </button>
                                  </div>
                                  <div class="modal-body">
                                    <p>{{this.details}}</p>
                                  </div>
                                  <div class="modal-footer">
                                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                                  </div>
                                </div>
                              </div>
                            </div>
                        </div>
                    </div>
                </div>
                {{/each}}
            </div>
        </div>
    </div>
</article> 
